import { MobileRequestStatus } from "@/components/client/MobileRequestStatus";

/** Guest mobile request tracker — `/guest/mobile-requests`. */
export default function GuestMobileRequests() {
  return <MobileRequestStatus />;
}
