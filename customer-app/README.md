# Not the Theramate customer app

This directory is an **Expo project for a different product** (Localito Marketplace — see `app.config.js` `name` / `slug`). It is **not** wired into the monorepo `package.json` workspaces and is **not** the Theramate / Peer Care Connect client.

**Theramate customer mobile (canonical):** [`../theramate-ios-client/`](../theramate-ios-client/README.md)

**Product docs (routes, parity, Supabase):** [`../docs/customer-app/README.md`](../docs/customer-app/README.md)

Do not confuse this folder with native Theramate work. If you only care about Theramate, work in `theramate-ios-client/` and ignore this tree unless you maintain Localito separately.
